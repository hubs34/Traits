input.onButtonPressed(Button.A, function () {
    previous_turn += -1
    basic.showNumber(previous_turn)
})
input.onButtonPressed(Button.AB, function () {
    previous_turn = 0
    if (true) {
    	
    }
})
input.onButtonPressed(Button.B, function () {
    previous_turn += 1
    basic.showNumber(previous_turn)
})
let previous_turn = 0
basic.showString("traits ")
previous_turn = 0
